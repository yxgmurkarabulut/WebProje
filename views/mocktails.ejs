<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mocktails | Budva Cafe</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
</head>

<body>

    <header class="header">
        <a href="/" style="text-decoration:none; color:black; font-size:14px; position:absolute; left:20px; top:45px;">
            <i class="fas fa-chevron-left"></i> Geri
        </a>

        <h1 class="brand-name">BUDVA</h1>
        <p style="font-size:14px; color:#00796b; font-weight:bold;">Mocktails & Özel İçecekler</p>

        <div class="cart-icon-container" onclick="sepetiAc()">
            <i class="fas fa-shopping-basket"></i>
            <span class="cart-count" id="sepetSayisi">0</span>
        </div>
    </header>

    <div class="menu-grid">
        <div class="menu-grid">
            <% if (urunler && urunler.length > 0) { %>
                <% urunler.forEach(function(urun) { %>
                    <div class="menu-item">
                        <img src="<%= urun.resim ? urun.resim : '/img/logo.png' %>" alt="<%= urun.ad %>">

                        <div class="menu-text">
                            <div style="font-weight:600;">
                                <%= urun.ad %>
                            </div>
                            <div style="font-size:15px; margin-top:22px; font-weight:bold;">
                                <%= urun.fiyat %> ₺</div>

                            <button class="add-btn" onclick="sepeteEkle('<%= urun.ad %>', <%= urun.fiyat %>, '<%= urun.resim %>')">
                                <i class="fas fa-plus"></i>
                            </button>
                        </div>
                    </div>
                    <% }); %>
                        <% } else { %>
                            <div style="grid-column: 1/-1; text-align: center; padding: 20px;">
                                <p>Henüz mocktail eklenmemiş.</p>
                            </div>
                            <% } %>
        </div>

        <div id="sepetModal" class="cart-modal">
            <div class="cart-content">
                <span class="close-btn" onclick="sepetiKapat()" style="float:right; font-size:24px; cursor:pointer;">&times;</span>
                <h2 style="color:#00796b;">Sepetim</h2>

                <div id="sepetIcerik" class="cart-items" style="margin-top:15px;"></div>

                <div style="display:flex; justify-content:space-between; margin: 20px 0; border-top: 1px solid #eee; padding-top: 15px;">
                    <span style="font-size:18px; font-weight:600;">Toplam:</span>
                    <span id="sepetToplam" class="total-price">0</span> ₺
                </div>

                <button class="checkout-btn" onclick="siparisVer()" style="width:100%; padding:12px; background:#00796b; color:white; border:none; border-radius:8px; cursor:pointer;">Siparişi Tamamla</button>
            </div>
        </div>

        <div id="bildirim-kutusu">Sepete Eklendi!</div>

        <script src="/js/script.js"></script>

</body>

</html>